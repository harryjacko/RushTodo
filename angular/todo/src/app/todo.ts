export class Todo {
	constructor(public id: number, public name: string, public completed: boolean, public remind_date, public repeat_remind: boolean, public remind_location: string, image: string){ }
}
